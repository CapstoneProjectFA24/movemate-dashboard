export enum PayemntMethodType {
  PAYOS = "PAYOS",
  VNPAY = "VNPAY",
  MOMO = "MOMO",
  WALLET = "WALLET",
}

export enum TransactionType {
  DEPOSIT = "DEPOSIT",
  RECEIVE = "RECEIVE",
  PAYMENT = "PAYMENT",
}

export enum statusType {
  SUCCESS = "SUCCESS",
}
